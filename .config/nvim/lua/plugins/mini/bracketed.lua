require("mini.bracketed").setup({
    comment = { suffix = "", options = {} },
    file = { suffix = "", options = {} },
    indent = { suffix = "", options = {} },
    yank = { suffix = "", options = {} },
})
